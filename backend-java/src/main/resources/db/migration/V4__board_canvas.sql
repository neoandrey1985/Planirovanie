-- v1.27: серверное хранение доски (бесконечный холст). Весь документ доски (камера + элементы)
-- хранится как JSON в одной строке — элементы разнородные, запросов по ним на сервере нет.
CREATE TABLE IF NOT EXISTS board_doc (
  id   INT PRIMARY KEY DEFAULT 1,
  data TEXT
);
