spring.datasource.url=jdbc:h2:mem:planirovanie;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=create-drop
# Tests build the H2 schema from entities; Flyway (PostgreSQL migrations) is disabled here.
spring.flyway.enabled=false
app.team-token=
